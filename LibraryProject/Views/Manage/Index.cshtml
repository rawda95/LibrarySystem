@model LibraryProject.Models.RegisterViewModel
@using Microsoft.AspNet.Identity;

@{
    /**/

    /**/

    ViewBag.Title = "Manage";
}
<div class="container my-5">
    <div class="row">
        <div class="col-12 col-md-4">
            <div>
                <img src="@ViewBag.imgSrc" width="200" height="200" />
                <br />
                @*<button class="btn btn-info mt-3">Edit Profile</button>*@
                @Html.ActionLink("Edit Profile", "Edit", "Account", new { id = User.Identity.GetUserId() }, new { @class = "btn btn-info mt-10" })
                @Html.ActionLink("Change your password", "ChangePassword", null, new { @class = "btn btn-info mt-10" })

                @*<button class="btn btn-primary mt-3">Change Password</button>*@
            </div>
        </div>
        <div class="col-12 col-md-8">
            <div>
                <table class="table table-hover mt-4 lead">
                    <tr>
                        <th>Frist name :</th>
                        <td>@Model.FirstName</td>
                    </tr>
                    <tr>
                        <th>last name :</th>
                        <td>@Model.LastName</td>
                    </tr>
                </table>
            </div>
        </div>
    </div>
